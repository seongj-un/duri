# 개발에서는 비워 둔다. Vite 프록시가 /api 와 /oauth2 를 백엔드로 넘긴다.
# 프론트를 백엔드와 다른 도메인에 올릴 때만 채운다 (그때는 쿠키 SameSite=None + Secure 도 필요).
VITE_API_BASE_URL=

# 개발 서버가 프록시할 백엔드 주소.
VITE_BACKEND_ORIGIN=http://localhost:8080
